/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['picsum.photos'], // Your image host domains
  },
  // Other config...
}

module.exports = nextConfig